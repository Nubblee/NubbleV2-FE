import React from "react";

const LoadingSpinner = () => {
  return (
    <div className="inline-block w-[50px] h-[50px] border-[5px] border-black/10 border-t-green-middle rounded-full animate-spin" />
  );
};

export default LoadingSpinner;
